# Entering PostgreSQL pod
kubectl exec -it postgres-db-86c86f648d-992bs -- bash

# Check number of records in EHR database through API

# Check dimension of EHR database
SELECT pg_size_pretty(pg_database_size(current_database()));